const express = require('express');
const cors = require('cors');
const app = express();
const PORT = 4000;
app.use(cors());
app.use(express.json());
app.get('/api/metrics', (req, res) => res.json({system: {name: "AINEON DASHBOARD", status: "í¿¢ OPERATIONAL"}, wallet: {totalBalance: 12847290, status: "SECURE"}, trading: {successRate: 98.7}}));
app.get('/health', (req, res) => res.json({status: "OK"}));
app.get('/', (req, res) => res.json({message: "íº€ AINEON MASTER DASHBOARD - PORT 4000"}));
app.listen(PORT, () => console.log(`âœ… MASTER DASHBOARD RUNNING: http://localhost:${PORT}`));
