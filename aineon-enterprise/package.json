{
    "name": "aineon-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "postinstall": "cd web && npm install",
        "build": "cd web && npm run build",
        "start": "cd web && npm run start"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}